# python-data-scanner
Pyhton tool that scans text files for sensitive data such as emails, credit card numbers and IP adresses.

The tool demonstrates how potentially sensitive information can be detected automatically using Python and regular expressions.
